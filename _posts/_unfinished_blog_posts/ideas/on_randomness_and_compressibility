
only infinite string can be truly random
truly random means can't be compressed
compression means abstraction information from data, pulling concepts from data
extent of compressibility defines how much is learnable
